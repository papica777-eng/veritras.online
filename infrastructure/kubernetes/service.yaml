apiVersion: v1
kind: Service
metadata:
  name: QANTUM-mind
  labels:
    app: QANTUM-mind
spec:
  type: ClusterIP
  selector:
    app: QANTUM-mind
  ports:
    - port: 80
      targetPort: 3000
      name: http
